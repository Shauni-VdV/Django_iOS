# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Django' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Django
  pod 'RxSwift', '~> 5.0.1'
  pod 'RxCocoa', '~> 5.0.1'
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SDWebImage'
  pod 'Moya'

  target 'DjangoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DjangoUITests' do
    # Pods for testing
  end

end
